[client]
# Connection to server (internal Docker hostname)
server_host = "http://server:8443"
private_key = "TWDhmcIowQnCyWB6OXSvh/RwncNqA3Baiv4UOizXHeY="       # Client private key
server_public_key = "4c81na95sfYK18g66ZZFdjW1Rjx1l8uHUq/5vrxSM9c="  # Server public key

[local]
host = "whoami"  # whoami container hostname
port = 80        # whoami service HTTP port

[reconnect]
enabled = true
initial_delay_ms = 1000
max_delay_ms = 30000  # Exponential backoff: 1s, 2s, 4s, 8s, 16s, 30s...
